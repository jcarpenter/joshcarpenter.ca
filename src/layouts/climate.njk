---js
{
    bibliography: "./climate-references.json",
    date: "Last Modified",
    colors: {
        primary: {
            //light: "120, 120, 120",
            dark: "124, 160, 204"
        }
    },
    eleventyComputed: {
        meta: {
            page_title: "{{ title }} | Josh Carpenter - Climate Notes",
            summary: "{{ summary or meta.summary }}",
            image: "{{ image or meta.image }}",
            type: "article"
        }
    },
}
---
<!doctype html>
<html lang="en">
    <head>
        {% include "head.njk" %}
    </head>
    <body class="climate" >
        <a href="#title" class="visibility-hidden">Skip to content</a>
        {% include "header.njk" %}
        <main role="main" aria-labelledby="title">
            
            {% if tags and tags.includes("explainer") %}
                <section id="explainer-forward">
                    <p>
                        This is a climate change explainer. I'm writing these for myself, as I try to better understand the science, impacts and potential solutions of climate change. Each explainer tries to sum up a specific topic, such as sea level rise. All explainers are works in progress.
                    </p>
                </section>
            {% endif %}

            <article id="post" {% if bibliography %}data-bibliography="{{ bibliography }}" {% endif %}>

                {# ------------ Header ------------ #}
                <header>
                    <h1 id="title">{{ title }}</h1>
                    <time datetime="{{ page.date }}">Updated {{ page.date | date("MMM D YYYY") }}</time>
                </header>

                {# ------------ Content ------------ #}
                <section aria-label="Main content body" id="content">
                    {{ content | safe }}
                </section>

            </article>

            {# {% include "related-content.njk" %} #}
            {% include "about-the-author.njk" %}
        </main>
        {% include "footer.njk" %}
    </body>
</html>