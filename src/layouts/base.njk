<!doctype html>
<html lang="en">
    <head>
        {% include "head.njk" %}
    </head>
    <body data-theme="none" {% if bibliography %}data-bibliography="{{ bibliography }}"{% endif %}>
        
        <a href="#title" class="visibility-hidden">Skip to content</a>
        {% include "navigation.njk" %}
        
        <main role="main" aria-labelledby="title" class="{{ post_layout }}">

            {{ content | safe }}

        </main>
        {% include "footer.njk" %}
        {% include "lightbox.njk" %}
    </body>
</html>